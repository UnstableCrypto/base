# `ingress-rpc`

JSON-RPC ingress server for the Unstable Stack.

Receives transactions and bundles from external clients, enqueues them to Kafka, and forwards metering responses back to connected builder nodes.
