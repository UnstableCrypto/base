# Base Common Precompiles

Base precompile definitions and fork-specific precompile sets.

This crate owns the Base-specific precompile schedule and input-limit overrides. Most EVM consumers should continue to use the `BasePrecompiles` re-export from `base-common-evm`.
